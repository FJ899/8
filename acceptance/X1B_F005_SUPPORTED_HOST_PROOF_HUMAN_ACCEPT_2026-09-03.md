# X1B F005 supported-host positive-path proof — Human authorization

Date: `2026-09-03`

Status: `HUMAN AUTHORIZATION / PROOF-ONLY / NO SCRIPTOPS EFFECT AUTHORITY`

## Human act

The Human response in the controlling conversation was exactly:

```text
accept
```

It accepted exactly the immediately preceding proposition:

```text
Authorize one bounded, inert F005 supported-host implementation positive-path proof.
```

## Bound lineage

- `FJ899/scriptops PR #35`
- BASE `2f22843ac570498b506101addeba5453ab777f08`
- repaired candidate HEAD `b281383be083be24d7e4b9f6c9411d3cc1c317f2`
- repaired candidate TREE `aa2b974efa01f55e0f909a0de60fcbde2b7e6a3f`
- `FJ899/8 PR #162` independent implementation re-review R2
- blocker `X1B-V2-IMPL-R2-F001 — F005 SUPPORTED-HOST LIVE GITHUB AUTHORITY POSITIVE-PATH EVIDENCE IS ABSENT`

## Authorized proof only

The authorized work may:

1. prepare deliberately non-executable V2 evidence in `FJ899/8` sufficient for the exact isolated GitHub/TLS child reader to exercise its public reviews/content path;
2. request a real GitHub pull-request review from the trusted Human account whose durable GitHub user ID is `226907434`;
3. after that separate Human GitHub review exists, execute the exact repaired candidate's isolated reader on a supported GitHub Actions Python 3.12 host;
4. capture evidence that the child receives only its executor-generated environment, loads nonempty default CA roots, uses hostname verification + `CERT_REQUIRED` + TLS >=1.2, performs a successful direct public `api.github.com` reviews GET without Authorization, observes the trusted Human `user.id = 226907434`, and fetches the immutable proof files at the review commit;
5. freeze the proof result for independent implementation re-review.

The proof evidence must be deliberately non-executable by ScriptOps. In particular it must bind `scriptops_base_head` to the all-zero 40-hex object ID so parent admission cannot match any real ScriptOps main commit.

## Explicit non-authority

This Human act does **not** authorize:

- ScriptOps `approve`;
- creation of a real executable X1B screenplay decision request;
- canonical `refs/heads/main` CAS;
- canonical screenplay or decision-log effect;
- treating the proof review itself as authority for a ScriptOps effect;
- mutation of the repaired ScriptOps candidate except proof infrastructure outside the frozen candidate;
- merge of PR #35 or any evidence/review PR;
- X1B closure;
- V1 authority;
- release, deployment or tag.

The real GitHub review required by the proof is a separate Human act on deliberately inert proof content. It is not screenplay execution authorization.

```text
AI PROPOSES != HUMAN DECIDES
REVIEW FINDING != PROOF AUTHORITY
PROOF REVIEW != SCRIPTOPS EFFECT AUTHORITY
GREEN LIVE PROOF != X1B CLOSURE
```
